apiVersion: v1
kind: Pod
metadata:
  name: latest-pod
  labels:
    team: devops
spec:
  containers:
    - name: nginx
      image: nginx:latest
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"
        requests:
          cpu: "200m"
          memory: "128Mi"